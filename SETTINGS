# -*- mode: Makefile; -*-

TTGSQL=dat/ttg.sql

BUILDMODES=debug release

TTGSRC=src

ifeq ($(shell uname -o),Cygwin)
FILES=$(TTGSQLITE3)
ISSSRC=iss
PKGSRC=$(ISSSRC)
else
ifneq ($(shell which dpkg),)
DEBSRC=deb
PKGSRC=$(DEBSRC)
else
PKGSRC=$(TTGSRC)
endif
FILES=
endif

TTGDPR=ttg.dpr
TTGLPR=ttg.lpr
