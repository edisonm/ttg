# -*- mode: Makefile; -*-

TTGSQL=dat/ttg.sql

TTGSQLINC=ttgsql.inc

BUILDMODES=full free

BUILDDATETIME=$(shell date +%a\ %b\ %e\ %H\\:%M\\:%S\ %Y)

ISSSRC=src/iss

TTGSRC=src/laz

ifeq ($(shell uname -o),Cygwin)
DBCONVERT=$(TTGDIR)/bin/dbconvert.exe
FILES=$(TTGSQLITE3)
CHAINSRC=$(ISSSRC)
else
FILES=
CHAINSRC=$(TTGSRC)
endif

TTGDPR=ttg.dpr
TTGLPR=ttg.lpr

