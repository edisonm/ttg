# -*- mode: Makefile; -*-

TTGSRC=../laz

include $(TTGSRC)/COMMON

ARCH=$(TargetCPU)
PKGBASE=ttg$(BuildLabel)_$(VERSION)_$(ARCH)
PKG=$(TTGDIR)/packages/$(PKGBASE).exe
FILES=$(PKG)

INNOIDE="$(PROGRAMFILES)/Inno Setup 5/ISCC.exe"
ISSTMPL=$(TTGDIR)/src/iss/ttg.iss.tmpl
ISS=$(OBJDIR)/ttg.iss
ISSWIN='$(shell cygpath -w $(ISS))'
