# -*- mode: Makefile; -*-

TTGSRC=../src

include $(TTGSRC)/COMMON

ARCH=$(shell dpkg --print-architecture)

PKGTTG=$(PKGDIR)/$(PKGBASE).deb
ifeq ($(BuildMode),release)
PKGTTG_DOC=$(PKGDIR)/$(PKGBASE_DOC).deb
PKGBASE_DOC=ttg-doc_$(VERSION)_all
endif

CATALOG=$(PKGDIR)/Packages.gz

PACKAGES=$(PKGTTG) $(PKGTTG_DOC)
FILES=$(PACKAGES) $(CATALOG)
DEBIAN=$(OBJDIR)/debian
